public boolean in1To10(int n, boolean outsideMode) {
  if (n >= 1 && outsideMode == false 
  && n <= 10 && outsideMode == false 
  || n >= 10 && outsideMode == true
  || n <= 1 &&  outsideMode == true) {
    return true;
  }
  
  return false;
}
